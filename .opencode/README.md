# OpenCode Plugin Notes

This directory holds OpenCode-specific repository-facing files such as install notes, plugin metadata, and other packaging-related docs when the format is confirmed.

Use `INSTALL.md` for bootstrap install status and deferred setup guidance.

Do not invent packaging files until the required OpenCode format has been verified.
