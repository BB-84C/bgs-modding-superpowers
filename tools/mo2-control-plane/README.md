# MO2 Control Plane

This subtree holds the MO2 control plane scaffold for agent-facing automation.

- `broker/` contains the broker CLI and protocol helpers.
- `plugin/` contains the in-process MO2 plugin kernel.
