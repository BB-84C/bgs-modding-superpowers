// MO2 MCP server entry point.
// Bootstrap implementation lands in Task S2.14 (S2b plan).
// This stub exists so npm run build succeeds for the skeleton.
console.error("mo2-mcp stub — bootstrap in Task S2.14");
process.exit(0);
