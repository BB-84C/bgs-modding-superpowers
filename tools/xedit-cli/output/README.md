# Output

This directory is reserved for wrapper-owned run artifacts such as SQLite databases, raw reports, and captured logs.

Treat these outputs as generated state, not hand-authored source files.
