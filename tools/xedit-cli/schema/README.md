# Schema

This directory documents the intermediate report schema and the SQLite tables that back Phase 1 drilldown queries.

The report schema should stay stable enough for wrapper ingestion without overcommitting future write workflows.
